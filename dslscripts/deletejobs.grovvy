import jenkins.*
import jenkins.model.*
import hudson.*
import hudson.model.*

for (job in Jenkins.instance.items) {
if (job instanceof Folder) {
println "This is a folder : " + job
} else {
println "This is a job : " + job
}
}
